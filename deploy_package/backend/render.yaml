services:
  - type: web
    name: anti-scam-agent-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python api_service.py
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
      - key: ANTHROPIC_API_KEY
        sync: false
